# Development placeholders only. Do not start services with these values.
# scripts/new-local-env.ps1 generates an ignored file without displaying secrets.
POSTGRES_USER=REPLACE_WITH_LOCAL_DATABASE_USER
POSTGRES_PASSWORD=REPLACE_WITH_RANDOM_LOCAL_DATABASE_PASSWORD
RABBITMQ_USER=REPLACE_WITH_LOCAL_BROKER_USER
RABBITMQ_PASSWORD=REPLACE_WITH_RANDOM_LOCAL_BROKER_PASSWORD
